test:
  pre:
    - bower i
    - gem install compass
  post:
    - git config --global user.name "circleci"
    - git config --global user.email "{{ git.email }}"
deployment:
  production:
    branch: master
    commands:
      - npm run deploy
machine:
  node:
    version: v0.10.33
